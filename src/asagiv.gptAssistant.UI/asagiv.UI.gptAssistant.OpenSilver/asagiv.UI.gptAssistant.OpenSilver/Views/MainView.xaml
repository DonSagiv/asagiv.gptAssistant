<UserControl x:Class="asagiv.UI.gptAssistant.OpenSilver.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:cc="clr-namespace:asagiv.UI.OpenSilver.CustomControls;assembly=asagiv.UI.OpenSilver"
             mc:Ignorable="d">

    <DockPanel>

        <DockPanel DockPanel.Dock="Bottom"
                   Height="150">

            <cc:AsagivButton Margin="10"
                             MinWidth="100"
                             Content="Submit"
                             DockPanel.Dock="Right"
                             Command="{Binding SubmitCommand}"/>

            <cc:AsagivTextBox Margin="10"
                              TextWrapping="Wrap"
                              KeyDown="PromptKeyDown"
                              TextAlignment="Left"
                              LabelText="Enter Prompt Here"
                              Text="{Binding TextBoxString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        </DockPanel>

        <Border Margin="10"
                BorderBrush="#FF718597"
                BorderThickness="1">

            <ItemsControl Margin="10"
                          ItemsSource="{Binding ChatHistory}"/>

        </Border>

    </DockPanel>

</UserControl>
